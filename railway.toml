[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn \"zotero_client.web:create_app()\" --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
